/**
 * Node half of the private Prompt Control browser package.
 *
 * The Host Loader needs this marker to retain the package's client declaration;
 * the browser implementation is exported from `./client`.
 */
export function apply(): void {}
